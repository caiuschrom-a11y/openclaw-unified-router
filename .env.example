# Required
STRIPE_SECRET_KEY=sk_live_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_AFTER_DEPLOY

# Recommended (no provider = emails go to filesystem outbox only)
RESEND_API_KEY=re_REPLACE_ME
EMAIL_FROM=openclaw <onboarding@resend.dev>

# Operator alerts (concierge orders, errors)
OPERATOR_EMAIL=oahudaddy@duck.com

# Only needed once SaaS api_key gateway is wired
ANTHROPIC_API_KEY=sk-ant-REPLACE_ME

# Persistence root (Railway gives ephemeral disk; mount a volume in prod)
PRODUCTS_DATA_DIR=/data
